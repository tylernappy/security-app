<table>
	<td>
		<dt>
			<div><%= @user.name %></div>
      <div><%= @user.email %></div>
			<div><%= @user.company %></div>
			<div><%= @user.title %></div>
			<div><%= @user.address %></div>
		</dt>
	</td>
</table>

<%= link_to "Back", events_path, :class => 'btn' %>
<%= link_to "Edit Profile", edit_user_path, :class => 'btn btn-primary' if current_user %>